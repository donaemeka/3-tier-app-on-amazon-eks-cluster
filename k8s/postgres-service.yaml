apiVersion: v1
kind: Service
metadata:
  name: db                     # Name of the Service
spec:
  selector:
    app: db                    # Connects to Pods labeled "app=db"
  ports:
    - port: 5432               # Service port (inside cluster)
      targetPort: 5432         # Forwards to Postgres container port
  # No NodePort → internal service, only worker & result apps use it
